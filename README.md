## 시작하기
```
npm install react-router-dom // 라우팅
npm install axios // 

npm install -D tailwindcss postcss autoprefixer // 자바스크립트로 css를 트랜스포밍해주는 툴
npx tailwindcss init -p // 테일윈드 사용

npm install victory // 차트
```
